version: "1"

tasks:
  - id: "example-task"
    title: "Example task"
    description: "Replace this with your actual task description"
    assignTo: "dev-1"
    dependsOn: []
    maxRetries: 3
    expectations:
      - type: "test"
        command: "npm test"
      - type: "file_exists"
        paths: ["src/index.ts"]
    metrics: []
