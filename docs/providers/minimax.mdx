---
title: "MiniMax"
sidebarTitle: "MiniMax"
description: "MiniMax's hosted models — available in global and China endpoints."
---

MiniMax provides hosted LLM models with both global and China-specific endpoints.

## Setup

```bash
export MINIMAX_API_KEY=...
```

### Config

```json
{
  "providers": {
    "minimax": "${MINIMAX_API_KEY}"
  }
}
```

### Use it

```json
{
  "agents": [
    { "name": "coder", "model": "minimax:minimax-m1" }
  ]
}
```

Polpo auto-infers `minimax` from the `minimax-` prefix.

## Models

| Model | Best for |
|-------|----------|
| `minimax-m1` | General-purpose tasks |

## Provider Details

| | |
|---|---|
| **Provider ID** | `minimax` |
| **Env variable** | `MINIMAX_API_KEY` |
| **API type** | MiniMax API |
| **Auto-infer prefix** | `minimax-` |

## China Endpoint

For users in China, use the `minimax-cn` provider:

```bash
export MINIMAX_CN_API_KEY=...
```

```json
{
  "providers": {
    "minimax-cn": "${MINIMAX_CN_API_KEY}"
  }
}
```
