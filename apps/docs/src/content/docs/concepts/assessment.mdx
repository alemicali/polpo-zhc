---
title: Result Evaluation
description: G-Eval LLM-as-judge scoring system for task quality
---

import { Aside } from '@astrojs/starlight/components';

## Overview

Polpo uses **G-Eval** (LLM-as-judge) to automatically assess completed tasks. Each task's output is evaluated against multiple dimensions, producing scores from 1-5 with chain-of-thought reasoning.

The assessment system runs an internal **8-phase pipeline**:

1. **Collect context** — gather task description, expectations, and agent output
2. **File existence checks** — verify required files exist
3. **Test/script execution** — run test commands and validation scripts
4. **Diff analysis** — analyze files created and edited by the agent
5. **LLM review** — G-Eval scoring across configured dimensions
6. **Score aggregation** — compute weighted global score
7. **Quality metrics collection** — record scores for quality gate evaluation and SLA tracking
8. **Result determination** — pass/fail decision with feedback generation

## Expectation Types

Tasks can define four types of expectations:

### `test` — Run a Test Command

```json
{
  "expectations": [
    {
      "type": "test",
      "command": "npm test"
    }
  ]
}
```

Runs the command and checks the exit code. Exit code 0 = pass.

### `file_exists` — Check File Presence

```json
{
  "expectations": [
    {
      "type": "file_exists",
      "paths": [
        "src/routes/users.ts",
        "src/routes/posts.ts"
      ]
    }
  ]
}
```

Verifies that the specified files exist in the working directory.

### `script` — Run a Custom Script

```json
{
  "expectations": [
    {
      "type": "script",
      "command": "node scripts/validate-schema.js"
    }
  ]
}
```

Similar to `test` but intended for custom validation scripts that check specific conditions.

### `llm_review` — AI-Powered Code Review

```json
{
  "expectations": [
    {
      "type": "llm_review",
      "criteria": "Verify proper error handling and input validation",
      "threshold": 3.5,
      "dimensions": [
        {
          "name": "security",
          "weight": 0.5,
          "rubric": "No SQL injection, XSS, or authentication bypass vulnerabilities"
        },
        {
          "name": "error_handling",
          "weight": 0.5,
          "rubric": "All error paths return proper HTTP status codes with messages"
        }
      ]
    }
  ]
}
```

The most powerful expectation type. An LLM reviews the agent's work against configurable dimensions.

## Default Dimensions

When no custom dimensions are specified, Polpo uses these defaults:

| Dimension | Weight | Description |
|-----------|--------|-------------|
| `correctness` | 0.35 | Task achieves its stated goals correctly |
| `completeness` | 0.30 | All requirements in the description are addressed |
| `code_quality` | 0.20 | Code is clean, readable, and maintainable |
| `edge_cases` | 0.15 | Edge cases and error conditions are handled |

## Scoring

Each dimension is scored **1-5** with the following scale:

| Score | Meaning |
|-------|---------|
| 1 | Completely wrong or missing |
| 2 | Major issues, barely functional |
| 3 | Acceptable but with notable gaps |
| 4 | Good quality with minor issues |
| 5 | Excellent, exceeds expectations |

### Global Score

The global score is a **weighted average** of all dimension scores:

```
globalScore = Σ (dimension.score × dimension.weight)
```

Tasks pass when `globalScore >= threshold` (default: 3.0).

### Chain-of-Thought Reasoning

For each dimension, the LLM generates step-by-step reasoning before scoring:

```json
{
  "dimension": "correctness",
  "score": 4,
  "reasoning": "The API endpoints correctly implement CRUD operations. GET /users returns paginated results. POST /users validates required fields. However, the PATCH endpoint doesn't handle partial updates correctly — it requires all fields.",
  "weight": 0.35
}
```

This reasoning is included in retry/fix feedback so agents know exactly what to improve.

## Assessment Flow

```
Task completes → Run expectations in order:
  1. file_exists checks
  2. test/script commands
  3. llm_review (if earlier checks pass)
→ Aggregate results
→ Collect quality metrics
→ Pass: task → done
→ Fail: task → fix phase (or retry)
```

<Aside>
Expectations are evaluated in order. If a `file_exists` or `test` check fails, the `llm_review` is skipped to save API calls. The agent receives specific feedback about which check failed.
</Aside>

## Retry with Feedback

When assessment fails, the feedback includes:

- **Which expectations failed** and why
- **Per-dimension scores** with reasoning
- **Specific suggestions** for improvement
- **Original task description** (preserved across retries)

The fix phase sends this feedback to the agent so it can make targeted corrections without starting from scratch.

## Custom Rubrics

For fine-grained control, define custom rubrics per dimension:

```json
{
  "dimensions": [
    {
      "name": "performance",
      "weight": 0.5,
      "rubric": {
        "1": "O(n²) or worse algorithms, no caching",
        "2": "Basic functionality but poor algorithmic choices",
        "3": "Reasonable performance, standard patterns",
        "4": "Good performance with appropriate caching",
        "5": "Optimal algorithms, efficient caching, lazy loading"
      }
    }
  ]
}
```

## Expectation Correction

Polpo includes an **expectation judge** that can detect when expectations themselves are wrong (not the agent's work). If the LLM determines that a failing expectation is unreasonable given the task, it can auto-correct the expectations and re-assess.

See [Expectation Judge](/guides/expectation-judge/) for details on auto-correction.

## Retry and Fix Phase

When assessment fails, Polpo uses a multi-stage retry system:

1. **Fix phase** — Send targeted feedback to the agent (up to `maxFixAttempts` attempts)
2. **Full retry** — Reset and start from scratch (up to `maxRetries` attempts)
3. **Escalation** — Switch to a different agent or model if configured via the escalation policy

After repeated assessment failures, the escalation policy can trigger automatic reassignment to a different agent, model upgrade, or human notification. See [Escalation Chain](/orchestration/escalation/) for escalation policy configuration and complete details on the retry flow.

## Integration with Quality Metrics

Assessment scores are collected and fed into the quality gate system. When a plan defines quality gates, the scores from assessed tasks are used to determine whether subsequent tasks can proceed. See [Plans](/concepts/plans/) for quality gate configuration.

## Configuration

```json
{
  "settings": {
    "maxFixAttempts": 2,
    "maxRetries": 3,
    "orchestratorModel": "claude-sonnet-4-5-20250929"
  }
}
```

Override per-task:

```json
{
  "tasks": [
    {
      "title": "Critical API endpoint",
      "agent": "backend-dev",
      "description": "...",
      "maxRetries": 5,
      "expectations": [
        {
          "type": "llm_review",
          "threshold": 4.0,
          "dimensions": []
        }
      ]
    }
  ]
}
```
