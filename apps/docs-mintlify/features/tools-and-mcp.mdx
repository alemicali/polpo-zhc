---
title: "Tools & MCP"
description: "7 core coding tools, 11 extended tool categories (52 tools total), filesystem sandboxing, and MCP server connections."
---

Agents need tools to do their job. Polpo gives every agent a set of **coding tools** for reading, writing, and searching code — **11 extended tool categories** for browser automation, git, HTTP, documents, and more — plus the ability to connect to external **MCP servers** for additional capabilities.

## Coding Tools (Core)

Polpo ships 7 built-in coding tools. They're the same primitives you'd expect from any code editor, designed for agents:

| Tool | What it does | Output limit |
|------|-------------|--------------|
| **`read`** | Read file contents with line numbers. Supports `offset` / `limit` for pagination. | 500 lines |
| **`write`** | Create or overwrite a file. Auto-creates parent directories. | — |
| **`edit`** | Search-and-replace in a file. `old_text` must match exactly once. | — |
| **`bash`** | Execute a shell command with a configurable timeout (default 120s). | 30 KB |
| **`glob`** | Find files matching a glob pattern (e.g. `**/*.ts`). | 200 results |
| **`grep`** | Regex search across files. Returns matching lines with file paths and line numbers. | 100 matches |
| **`ls`** | List a directory. Directories are suffixed with `/`. | — |

All tools except `bash` enforce the **filesystem sandbox** (see below). The `bash` tool runs commands from the project working directory.

<Info>
For full parameter schemas, return types, error codes, and usage examples for each core tool, see the [File System & Coding reference](/reference/coding-tools).
</Info>

### Restricting Tools

By default, agents get all 7 core tools. You can restrict this per agent:

```json
{
  "name": "read-only-reviewer",
  "role": "code reviewer",
  "allowedTools": ["read", "glob", "grep", "ls"]
}
```

This agent can browse code but can't write, edit, or run commands — useful for review-only roles.

## Extended Tools

Beyond the 7 core tools, Polpo offers **11 optional tool categories** with **52 extended tools** total. Each category is enabled per-agent with a config flag.

### Browser (18 tools)

Automate browser interactions — navigate, click, fill forms, take screenshots, extract data. Powered by the `agent-browser` CLI.

```json
{ "name": "qa-agent", "enableBrowser": true }
```

Key tools: `browser_navigate`, `browser_click`, `browser_type`, `browser_screenshot`, `browser_evaluate`, `browser_wait`, `browser_select`, `browser_scroll`, `browser_drag_drop`, and more.

<Card title="Browser tools reference" icon="arrow-right" href="/reference/tools/browser">
  Full parameter schemas and usage for all 18 browser tools.
</Card>

### Git (7 tools)

Structured git operations with safe environment (API keys stripped from subprocess env).

```json
{ "name": "dev-agent", "enableGit": true }
```

Key tools: `git_status`, `git_diff`, `git_log`, `git_commit`, `git_branch`, `git_checkout`, `git_push`.

<Card title="Git tools reference" icon="arrow-right" href="/reference/tools/git">
  Full parameter schemas and usage for all 7 git tools.
</Card>

### HTTP (2 tools)

Make HTTP requests and download files — useful for API testing, fetching data, or downloading assets.

```json
{ "name": "api-agent", "enableHttp": true }
```

Tools: `http_request`, `http_download`.

<Card title="HTTP tools reference" icon="arrow-right" href="/reference/tools/http">
  Full parameter schemas and usage for HTTP tools.
</Card>

### Multi-file (3 tools)

Batch operations across multiple files — regex replace, bulk rename, batch edits.

```json
{ "name": "refactor-agent", "enableMultifile": true }
```

Tools: `multifile_replace`, `multifile_rename`, `multifile_edit`.

<Card title="Multi-file tools reference" icon="arrow-right" href="/reference/tools/multifile">
  Full parameter schemas and usage for multi-file tools.
</Card>

### Dependencies (6 tools)

Package management across npm, pnpm, yarn, and bun.

```json
{ "name": "setup-agent", "enableDeps": true }
```

Key tools: `deps_install`, `deps_add`, `deps_remove`, `deps_update`, `deps_list`, `deps_audit`.

<Card title="Dependencies tools reference" icon="arrow-right" href="/reference/tools/deps">
  Full parameter schemas and usage for all 6 dependency tools.
</Card>

### Excel/CSV (4 tools)

Read, write, and query spreadsheets and CSV files.

```json
{ "name": "data-agent", "enableExcel": true }
```

Tools: `excel_read`, `excel_write`, `excel_query`, `excel_info`.

<Card title="Excel/CSV tools reference" icon="arrow-right" href="/reference/tools/excel">
  Full parameter schemas and usage for spreadsheet tools.
</Card>

### PDF (4 tools)

Read, create, merge, and inspect PDF documents.

```json
{ "name": "doc-agent", "enablePdf": true }
```

Tools: `pdf_read`, `pdf_create`, `pdf_merge`, `pdf_info`.

<Card title="PDF tools reference" icon="arrow-right" href="/reference/tools/pdf">
  Full parameter schemas and usage for PDF tools.
</Card>

### Word/DOCX (2 tools)

Read and create Microsoft Word documents.

```json
{ "name": "doc-agent", "enableDocx": true }
```

Tools: `docx_read`, `docx_create`.

<Card title="Word/DOCX tools reference" icon="arrow-right" href="/reference/tools/docx">
  Full parameter schemas and usage for Word document tools.
</Card>

### Email (2 tools)

Send emails via SMTP — useful for notification agents or report distribution.

```json
{ "name": "notifier", "enableEmail": true }
```

Tools: `email_send`, `email_send_html`.

<Card title="Email tools reference" icon="arrow-right" href="/reference/tools/email">
  Full parameter schemas and usage for email tools.
</Card>

### Audio (2 tools)

Speech-to-text transcription and text-to-speech synthesis.

```json
{ "name": "voice-agent", "enableAudio": true }
```

Tools: `audio_transcribe`, `audio_synthesize`.

<Card title="Audio tools reference" icon="arrow-right" href="/reference/tools/audio">
  Full parameter schemas and usage for audio tools.
</Card>

### Image (2 tools)

AI image generation and image analysis/description.

```json
{ "name": "creative-agent", "enableImage": true }
```

Tools: `image_generate`, `image_analyze`.

<Card title="Image tools reference" icon="arrow-right" href="/reference/tools/image">
  Full parameter schemas and usage for image tools.
</Card>

### Summary

| Category | Tools | Config Flag | Description |
|----------|:-----:|-------------|-------------|
| [Browser](/reference/tools/browser) | 18 | `enableBrowser` | Browser automation via `agent-browser` CLI |
| [Git](/reference/tools/git) | 7 | `enableGit` | Structured git operations with safe environment |
| [HTTP](/reference/tools/http) | 2 | `enableHttp` | HTTP requests and file downloads |
| [Multi-file](/reference/tools/multifile) | 3 | `enableMultifile` | Batch edits, regex replace, bulk rename |
| [Dependencies](/reference/tools/deps) | 6 | `enableDeps` | Package management (npm/pnpm/yarn/bun) |
| [Excel/CSV](/reference/tools/excel) | 4 | `enableExcel` | Read, write, and query spreadsheets |
| [PDF](/reference/tools/pdf) | 4 | `enablePdf` | Read, create, merge, inspect PDFs |
| [Word/DOCX](/reference/tools/docx) | 2 | `enableDocx` | Read and create Word documents |
| [Email](/reference/tools/email) | 2 | `enableEmail` | Send emails via SMTP |
| [Audio](/reference/tools/audio) | 2 | `enableAudio` | Speech-to-text and text-to-speech |
| [Image](/reference/tools/image) | 2 | `enableImage` | AI image generation and analysis |

### Enabling Multiple Categories

You can enable multiple categories on a single agent:

```json
{
  "name": "full-stack-dev",
  "enableBrowser": true,
  "enableGit": true,
  "enableHttp": true,
  "enableMultifile": true,
  "enableDeps": true
}
```

## Filesystem Sandbox

Agents are sandboxed by default to the **project root directory**. They can't read or write files outside it. You can tighten this further with `allowedPaths`:

```json
{
  "name": "frontend-dev",
  "allowedPaths": ["./src/ui", "./public", "./tests/ui"]
}
```

Now this agent can only touch files in those three directories (and their subdirectories). Any attempt to access `./src/api/` or `../` will be rejected with a clear error.

This is path-prefix matching with separator awareness — `./src/ui` allows `./src/ui/Button.tsx` but not `./src/ui-legacy/old.js`.

### How It Works

1. Paths are resolved to absolute paths at tool creation time
2. When no `allowedPaths` are set, the default is `[cwd]` (project root)
3. Every file operation checks `isPathAllowed()` before proceeding
4. Violations throw a descriptive error that tells the agent what it tried and where it's allowed

## Safe Environment

When agents run shell commands (`bash` tool) or spawn MCP servers, Polpo filters the environment variables to prevent leaking API keys and secrets.

The `safeEnv()` function only passes through system-essential variables:

- System: `PATH`, `HOME`, `USER`, `SHELL`, `TERM`, `LANG`, `NODE_ENV`
- Git: `GIT_AUTHOR_NAME`, `GIT_COMMITTER_NAME`, `GIT_SSH_COMMAND`, etc.
- Network: `HTTP_PROXY`, `HTTPS_PROXY`, `NO_PROXY`
- SSH: `SSH_AUTH_SOCK`

Everything else — `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, database passwords, custom secrets — is stripped. This means agent subprocesses can't exfiltrate your secrets via tool calls.

See [Security](/features/security) for more details on environment filtering.

## MCP (Model Context Protocol)

Agents can connect to external [MCP](https://modelcontextprotocol.io/) servers for additional capabilities — databases, APIs, custom tools, anything with an MCP interface.

<Note>
Polpo is an MCP **client**, not a server. When you configure `mcpServers` on an agent, you're telling Polpo which external MCP servers to connect to. The agent discovers tools from those servers and can call them alongside the built-in coding tools.
</Note>

Polpo supports all three MCP transport types:

<Tabs>
  <Tab title="stdio">
    ```json
    {
      "name": "dev-with-db",
      "mcpServers": {
        "postgres": {
          "command": "npx",
          "args": ["-y", "@modelcontextprotocol/server-postgres", "postgresql://localhost/mydb"],
          "env": { "NODE_ENV": "development" }
        }
      }
    }
    ```
    The server is spawned as a child process using `safeEnv()` — your API keys won't leak to the MCP server process.
  </Tab>

  <Tab title="SSE">
    ```json
    {
      "name": "dev-with-remote-tools",
      "mcpServers": {
        "my-tools": {
          "type": "sse",
          "url": "https://my-mcp-server.example.com/sse",
          "headers": { "Authorization": "Bearer token123" }
        }
      }
    }
    ```
  </Tab>

  <Tab title="HTTP (Streamable)">
    ```json
    {
      "name": "dev-with-http-tools",
      "mcpServers": {
        "my-tools": {
          "type": "http",
          "url": "https://my-mcp-server.example.com/mcp",
          "headers": { "Authorization": "Bearer token123" }
        }
      }
    }
    ```
  </Tab>
</Tabs>

Tools discovered from connected MCP servers are added to the agent's tool set alongside the built-in coding tools. The agent doesn't know (or care) whether a tool is built-in or comes from an MCP server — it just uses what's available.
