---
title: "Plans"
description: "Define multi-task execution plans"
---

## What is a Plan?

A **plan** is a group of related tasks that Polpo executes together. Plans are stored as JSON in `.polpo/plans/` and can include:

- Multiple tasks with dependencies
- Volatile teams (temporary agents scoped to the plan)
- Custom assessment criteria per task
- Quality gates that block progression until thresholds are met
- Cron-based scheduling for recurring execution
- Deadlines tracked by SLA monitoring

## Plan Lifecycle

<Frame>
  <img src="/plan-lifecycle.svg" alt="Plan lifecycle: draft → active → completed / failed / cancelled" />
</Frame>

| Status | Description |
|--------|-------------|
| `draft` | Plan created but not yet executed |
| `active` | Tasks are being processed |
| `completed` | All tasks finished successfully |
| `failed` | One or more tasks failed after exhausting retries |
| `cancelled` | Plan was manually aborted |

## Creating Plans

Plans are created via CLI, TUI, or API — not by editing the config file.

<Tabs>
  <Tab title="CLI">
    ```bash
    # Generate a plan with AI
    polpo plan create "Build a REST API with auth, rate limiting, and tests"

    # Review the plan
    polpo plan list
    polpo plan show <plan-id>

    # Execute it
    polpo plan execute <plan-id>
    ```
  </Tab>

  <Tab title="TUI">
    ```
    /plan
    ```
    The TUI plan command lets you create plans from templates or generate them with AI.
  </Tab>

  <Tab title="HTTP API">
    ```bash
    curl -X POST http://localhost:3000/api/v1/projects/my-project/plans \
      -H "Content-Type: application/json" \
      -d '{"prompt": "Build a REST API with auth"}'

    curl -X POST http://localhost:3000/api/v1/projects/my-project/plans/<plan-id>/execute
    ```
  </Tab>
</Tabs>

## Plan Task Format

Each task inside a plan uses these fields:

```json
{
  "title": "Implement JWT middleware",
  "description": "Build Express middleware for JWT authentication.",
  "assignTo": "auth-dev",
  "dependsOn": ["Design auth schema"],
  "expectations": [
    {
      "type": "test",
      "command": "npm test -- --grep auth",
      "weight": 0.6
    },
    {
      "type": "llm_review",
      "prompt": "Verify tests cover login, logout, token refresh, and invalid tokens",
      "weight": 0.4
    }
  ]
}
```

| Field | Type | Description |
|-------|------|-------------|
| `title` | string | Short task title (used as dependency reference) |
| `description` | string | Detailed instructions for the agent |
| `assignTo` | string | Agent name from your team. Defaults to first agent if omitted |
| `dependsOn` | string[] | Titles of tasks that must finish first |
| `expectations` | array | Assessment criteria |
| `maxRetries` | number | Override default retry limit |
| `maxDuration` | number | Timeout in ms |
| `retryPolicy` | object | Escalation settings (`escalateAfter`, `fallbackAgent`) |

## Dependencies

Tasks can depend on other tasks by referencing their titles using the `dependsOn` field:

```json
{
  "tasks": [
    {
      "title": "Build database layer",
      "assignTo": "backend-dev",
      "description": "Create SQLite database with users and posts tables"
    },
    {
      "title": "Build REST API",
      "assignTo": "backend-dev",
      "description": "Create Express routes for CRUD operations",
      "dependsOn": ["Build database layer"]
    }
  ]
}
```

<Note>
Dependencies are matched by **task title** within the same plan group. A task won't start until all its dependencies reach `done` status.
</Note>

## Volatile Teams

Plans can define agents that exist only for that plan:

```json
{
  "team": [
    {
      "name": "migration-expert",
      "role": "Database migration specialist",
      "volatile": true
    }
  ],
  "tasks": [
    {
      "title": "Migrate legacy data",
      "assignTo": "migration-expert",
      "description": "Convert old JSON files to new format"
    }
  ]
}
```

When the plan completes (all tasks done or failed), Polpo automatically removes volatile agents. This prevents agent sprawl when running many specialized plans.

### Cleanup Modes

```json
{
  "settings": {
    "enableVolatileTeams": true,
    "volatileCleanup": "on_complete"
  }
}
```

- `on_complete`: Agents removed immediately when their plan finishes
- `manual`: Agents persist until explicitly removed

## Quality Gates

Quality gates block plan progression until preceding tasks meet a score threshold:

```json
{
  "qualityGates": [
    {
      "name": "design-review",
      "afterTasks": ["Design API schema", "Design database schema"],
      "minScore": 4.0
    },
    {
      "name": "implementation-review",
      "afterTasks": ["Implement API endpoints", "Implement database layer"],
      "minScore": 3.5
    }
  ],
  "tasks": [
    { "title": "Design API schema", "assignTo": "architect", "description": "..." },
    { "title": "Design database schema", "assignTo": "architect", "description": "..." },
    { "title": "Implement API endpoints", "assignTo": "backend-dev", "description": "...", "dependsOn": ["Design API schema"] },
    { "title": "Implement database layer", "assignTo": "backend-dev", "description": "...", "dependsOn": ["Design database schema"] },
    { "title": "Write integration tests", "assignTo": "backend-dev", "description": "...", "dependsOn": ["Implement API endpoints", "Implement database layer"] }
  ]
}
```

Tasks after a quality gate won't start until all gate tasks are `done` and their average assessment score meets `minScore`. The `quality:gate` hook fires when a gate is evaluated.

## Scheduling

Plans can be scheduled using cron expressions:

```json
{
  "schedule": "0 2 * * *",
  "recurring": true,
  "tasks": [
    {
      "title": "Run security audit",
      "assignTo": "security-dev",
      "description": "Scan codebase for vulnerabilities and generate report"
    }
  ]
}
```

| Field | Type | Description |
|-------|------|-------------|
| `schedule` | string | Cron expression (e.g., `"0 2 * * *"` for daily at 2 AM) |
| `recurring` | boolean | If `true`, the plan re-executes on each trigger. If `false` (default), it runs once at the next scheduled time. |

Scheduling requires `"enableScheduler": true` in the top-level config. The `schedule:trigger` hook fires when a scheduled plan is triggered.

## Execution

Plans can be executed via:

<Tabs>
  <Tab title="CLI">
    ```bash
    polpo plan execute <plan-id>
    polpo run    # executes all pending plans
    ```
  </Tab>

  <Tab title="TUI">
    ```
    /plan
    ```
    The TUI plan command lets you create plans from templates or generate them with AI.
  </Tab>

  <Tab title="HTTP API">
    ```bash
    curl -X POST http://localhost:3000/api/v1/projects/my-project/plans/<plan-id>/execute
    ```
  </Tab>

  <Tab title="Web UI">
    Navigate to the Plans page and click **Execute** on a draft plan.
  </Tab>
</Tabs>

## Plan Reports

When a plan completes, Polpo generates a **PlanReport** with:

```typescript
interface PlanReport {
  planId: string;
  group: string;
  allPassed: boolean;
  totalDuration: number;        // sum of all task durations (ms)
  tasks: {
    title: string;
    status: "done" | "failed";
    duration: number;
    score?: number;             // G-Eval score (1-5)
    filesCreated: string[];
    filesEdited: string[];
  }[];
  filesCreated: string[];       // aggregated across all tasks
  filesEdited: string[];
  avgScore?: number;            // average assessment score
}
```

Plan completion triggers the `plan:completed` event with the full report.

## AI-Generated Plans

Polpo can generate plans from natural language prompts:

```bash
polpo plan create "Build a REST API with authentication, rate limiting, and tests"
```

Or interactively in the TUI:

```
/plan
> Generate with AI
> Prompt: Build a REST API with authentication, rate limiting, and tests
```

The AI generates a plan with appropriate agents, tasks, dependencies, and expectations. You can then review, edit, and execute it.

## Editing Running Plans

The `/edit-plan` TUI command lets you modify active plans:

- **Add tasks** to a running plan
- **Remove** pending tasks
- **Reassign** tasks to different agents
- **Retry** specific failed tasks
